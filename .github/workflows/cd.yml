name: CD

on:
  push:
    branches: [main, master]
    tags: ["v*"]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  ##############################################################################
  # Build & push Docker image to GitHub Container Registry
  ##############################################################################
  push:
    name: Build & Push Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=ref,event=branch
            type=sha,prefix=,suffix=,format=short

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # NOTE: The automated deploy job is DISABLED.
  # The previous implementation rendered docker-compose.deploy.yml on the
  # GitHub runner but never transferred it to the VPS, and did not handle the
  # nginx template / TLS certs. Use scripts/deploy_vps.sh on the VPS instead.
  # To re-enable: implement a working transfer + var handling, then uncomment.
  # ##############################################################################
  # # Deploy to production (example: SSH + docker compose pull && up)
  # ##############################################################################
  # deploy:
  #   name: Deploy
  #   runs-on: ubuntu-latest
  #   needs: push
  #   if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
  #
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Render docker-compose.prod.yml
  #       env:
  #         VERSION: ${{ github.sha }}
  #         POSTGRES_USER: ${{ secrets.POSTGRES_USER }}
  #         POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
  #         POSTGRES_DB: ${{ secrets.POSTGRES_DB }}
  #         SECRET_KEY: ${{ secrets.SECRET_KEY }}
  #         JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
  #         GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
  #         REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
  #         GRAFANA_ADMIN_PASSWORD: ${{ secrets.GRAFANA_ADMIN_PASSWORD }}
  #       run: |
  #         envsubst < docker-compose.prod.yml > docker-compose.deploy.yml
  #
  #     - name: Deploy via SSH
  #       uses: appleboy/ssh-action@v1
  #       with:
  #         host: ${{ secrets.DEPLOY_HOST }}
  #         port: ${{ secrets.DEPLOY_PORT || '22' }}
  #         username: ${{ secrets.DEPLOY_USER }}
  #         key: ${{ secrets.DEPLOY_SSH_KEY }}
  #         script: |
  #           cd /opt/timetravel
  #           ghcr_token=$(curl -s "https://ghcr.io/token?service=ghcr.io&scope=repository:${{ github.repository }}:pull" | jq -r '.token')
  #           echo "$ghcr_token" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
  #           docker compose -f docker-compose.deploy.yml pull
  #           docker compose -f docker-compose.deploy.yml up -d --remove-orphans
  #           docker image prune -f
