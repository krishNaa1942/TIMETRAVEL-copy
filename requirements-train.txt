# Train-time only dependencies (scripts/train_models.py, scripts/evaluate_models.py).
# The runtime only needs scikit-learn (already in requirements.txt).
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
numpy>=1.24,<3.0
